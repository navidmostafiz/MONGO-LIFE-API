var count = 0;
module.exports = function broadcast(msg, data) {
    // count++;
    // console.log('*' + count + '* ' + msg);
    // if (!data == null) { console.log('*' + count + '* ', data); }
    console.log('* ' + msg);
}